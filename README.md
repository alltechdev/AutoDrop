# AutoDrop

Modified version of [Fossify Dialer](https://github.com/FossifyOrg/Phone) that silently rejects all incoming calls.

## Build

```
./gradlew assembleRelease
```

## Requirements

Must be set as the default (and only) phone app on the device.
